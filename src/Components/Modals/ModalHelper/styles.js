import styled from 'styled-components';

export const ContainerModal = styled.div`
  width: 560px;
  margin: 0 auto;
  position: fixed;
  left: 50%;
  top: 5%;
  z-index: 1000;
  transform: translateX(-50%);
`;
